import { } from '../gen/types'

const merge =
  (obj1: any) =>
    (obj2: any) => (
      { ...obj1, ...obj2 }
    )

const pythonCode =
  (type: any)

export const args =
  (args: any) => (
    
  )
